{
  "name": "vesta-ui",
  "version": "1.0.0",
  "description": "Librería de UX/UI independiente de frameworks",
"scripts": {
  "build": "if exist dist rmdir /s /q dist && mkdir dist && xcopy /E /I src dist",
"start": "npx http-server ./ -p 0"
}

,
  "devDependencies": {
    
  },
  "dependencies": {
    "@types/jquery": "^3.5.32",
    "jquery": "^3.7.1"
  }
}
